name: dependency_checking
description: Checking for required tools, files, or packages before proceeding
when_to_use:
- require
- need
- check
- dependency
instruction: '- Verify required commands exist using which or command -v

  - Check for required files before operating on them

  - Install missing dependencies when appropriate

  - Report missing dependencies clearly to the user'
