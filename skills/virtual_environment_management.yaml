name: virtual_environment_management
description: Creating and using Python virtual environments
when_to_use:
- venv
- virtualenv
- environment
- pip install
instruction: '- Create venv in a folder named venv or .venv

  - Always activate before installing packages

  - Document the Python version used

  - Include requirements.txt when creating reusable projects'
