# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
dist/
.DS_Store

# Environment
.env
venv/
env/

# Secrets
*.key
*.pem
*_secret*
*_password*

# Large files
*.db
*.safetensors
*.bin
*.gguf
qwen-tooluse-lora/

# Logs
*.log
model-telemetry/
*.model-telemetry.jsonl

# Generated reports
output/pdf/

# Benchmark outputs (contain dynamic system state)
benchmark_results.json
.a8088_verify.png
traces/
skillopt_data/

# Local tooling (graphify knowledge graph + opencode config)
graphify-out/
.opencode/

# Local agent instructions (graphify + ponytail tool prefs). Still tracked, so
# git keeps reporting changes to it — this entry only takes effect once the file
# is untracked with `git rm --cached AGENTS.md`.
AGENTS.md

# Config snapshots (may contain secrets - never commit)
backup.txt
opencode.json

# WhatsApp bridge npm deps
src/agent8088/gateway/platforms/whatsapp_bridge/node_modules/

# Test and run artifacts — generated files never belong in the repo root
artifacts/*
!artifacts/.gitkeep
