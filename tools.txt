execute_shell|Execute a shell command and return output|mode=shell|command={command}|timeout=25
write_file|Write content to a file|mode=write_text|path_arg=filename|content_arg=content|timeout=10
read_text|Read text from a file|mode=read_text|path_arg=filename|timeout=10
web_search|Search the web for information|mode=http_get|url={search_base_url}{query_q}&format=json|timeout=25
get_page_title|Fetch a webpage and extract its title|mode=shell|command=curl -s --max-time 15 "{url}" | grep -oP '(?<=<title>).*?(?=</title>)' | head -1|timeout=20
calculate|Evaluate a mathematical expression|mode=python_eval|expression={expression}|timeout=10
last_output|Get full output from the last tool call|mode=last_output
spawn_subagent|Delegate a self-contained task to an independent sub-agent that runs its own tool-using loop with fresh context and returns a concise summary. Use for multi-step research or search that would clutter the main context. Args: agent_type (e.g. general-purpose, explore), task (the full standalone instruction).|mode=subagent|args=agent_type,task|timeout=300
git_status|Show git status and current branch for the working repo|mode=shell|command=git status --short --branch|timeout=20
git_diff|Show the current git diff|mode=shell|command=git diff|timeout=20
git_log|Show recent commit history|mode=shell|command=git log --oneline -20|timeout=20
git_clone|Clone a repository into a directory|mode=shell|args=url,directory|command=git clone {url} {directory}|timeout=180
git_commit|Stage all changes and commit with a message. Only use when the user asked you to commit.|mode=shell|args=message|command=git add -A && git commit -m "{message}"|timeout=30
git_push|Push the current branch to origin. Only use when the user asked you to push.|mode=shell|command=git push origin HEAD|timeout=120
git_create_pr|Open a pull request with the gh CLI. Only use when the user asked for a PR.|mode=shell|args=title,body|command=gh pr create --title "{title}" --body "{body}"|timeout=120