# Core dependencies
openai>=1.0.0
httpx>=0.24.0
rich>=13.0.0

# Optional dependencies for training
torch>=2.0.0
transformers>=4.35.0
peft>=0.5.0
datasets>=2.14.0
accelerate>=0.24.0
bitsandbytes>=0.41.0

# Memory and vector search
lancedb>=0.3.0

# Web search integration
requests>=2.31.0

# Testing (dev)
pytest>=7.0

# --- Optional extras (the harness runs fine without these) ---
# browse_page tool — real headless browsing of JS-rendered pages:
#   pip install playwright && playwright install chromium
# playwright>=1.40.0
#
# run_sandboxed tool — needs the Docker daemon running (no pip package).
