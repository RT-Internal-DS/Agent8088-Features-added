execute_shell|Execute a necessary local shell command and return its real output. When the user asks to run a command, call this tool; do not merely describe or predict the result. Never use it for web research, current facts, or arithmetic.|mode=shell|args=command|command={command}|timeout=25
write_file|Write content to a file|mode=write_text|args=filename,content|path_arg=filename|content_arg=content|timeout=10
read_text|Read a file as text. Word, Excel, PowerPoint and PDF files (.docx, .xlsx, .pptx, .pdf) are extracted to readable text automatically. Long files come back one page at a time with a header saying how many lines there are; pass offset to read on from there, and limit to change the page size.|mode=read_text|args=filename,offset,limit|optional=offset,limit|arg_types=offset:integer,limit:integer|path_arg=filename|timeout=10
web_search|Search the web for fresh information. Always use it before answering about current leaders, releases, prices, availability, schedules, news, vulnerabilities, recommendations, or exchange rates. Routes to the configured backend (SearXNG by default, Tavily or Exa when an API key is set, keyless ddgs as fallback) and falls back automatically when one is unavailable.|mode=search|args=query|timeout=30
get_page_title|Fetch a webpage and extract its title|mode=http_get|args=url|url={url}|extract=title|timeout=20
calculate|Evaluate a mathematical expression|mode=python_eval|args=expression|expression={expression}|timeout=10
last_output|Get full output from the last tool call|mode=last_output
describe_capabilities|Report your own tools, MCP servers, skills, subagents, permission mode, sandbox backend, and active guardrails. Call this whenever the user asks what tools, MCP servers, features, limits, or capabilities you have — answer from this report instead of guessing. Takes no arguments.|mode=introspect|timeout=10
spawn_subagent|Delegate a self-contained task to an independent sub-agent that runs its own tool-using loop with fresh context and returns a concise summary. Use for multi-step research or search that would clutter the main context. Args: agent_type (e.g. general-purpose, explore), task (the full standalone instruction).|mode=subagent|args=agent_type,task|timeout=300
create_subagent|Create or update a custom sub-agent profile that spawn_subagent can then delegate to. Writes a markdown profile in the user's agents directory. Args: name, description, tools (comma-separated tool names), max_turns, model (optional - must be a model the active provider offers; omit to use the session model), prompt (the sub-agent's system prompt).|mode=write_text|args=name,description,tools,max_turns,model,prompt|timeout=10
present_plan|Present a finished plan to the user for approval. Use this in plan mode as soon as you know what to do. Pass the whole plan as markdown text in "plan": the goal, numbered steps, and the files each step touches. The user approves it, the permission mode changes, and you then carry out the steps with ordinary tool calls. Do NOT pass JSON steps and do NOT use this outside plan mode.|mode=plan|args=plan|timeout=600
execute_plan|Execute an already-decided sequence of tool calls, each verified before the next runs. This is NOT how you propose a plan in plan mode — use present_plan for that. Pass steps as a JSON string array of objects, each with "tool" and "arguments" keys. A step may also declare "acceptance" (what must be true for the step to count as done) and "evidence" (what to inspect to check it); state them for anything whose success is not obvious from the tool's own output. Execution stops at the first step that fails. Example: steps='[{"tool":"write_file","arguments":{"filename":"C:/tmp/x.txt","content":"hi"},"acceptance":"x.txt contains hi","evidence":"read the file back"}]'|mode=plan|args=steps|timeout=300
git_status|Show git status and current branch for the working repo|mode=shell|command=git status --short --branch|timeout=20|host=1
git_diff|Show the current git diff|mode=shell|command=git diff|timeout=20|host=1
git_log|Show recent commit history|mode=shell|command=git log --oneline -20|timeout=20|host=1
git_clone|Clone a repository into a directory|mode=shell|args=url,directory|command=git clone {url} {directory}|timeout=180|host=1
git_commit|Stage all changes and commit with a message. Only use when the user asked you to commit.|mode=shell|args=message|command=git add -A && git commit -m "{message}"|timeout=30|host=1
git_push|Push the current branch to origin. Only use when the user asked you to push.|mode=shell|command=git push origin HEAD|timeout=120|host=1
git_create_pr|Open a pull request with the gh CLI. Only use when the user asked for a PR.|mode=shell|args=title,body|command=gh pr create --title "{title}" --body "{body}"|timeout=120|host=1
schedule_task|Schedule this agent to run a task periodically, or list/remove schedules. action is list, add, or remove; schedule is 5 cron fields (e.g. "0 9 * * *"); task is the query to run. Windows uses Task Scheduler while the user is signed in and supports common minute, hourly, daily, weekly, and monthly forms.|mode=cron|args=action,schedule,task|optional=schedule,task|timeout=20
run_sandboxed|Run a Python snippet with native OS isolation and no network, using Docker only as a fallback. Use for untrusted or risky code instead of execute_shell.|mode=docker|args=code|timeout=90
view_skill|Load one text resource from an installed progressive skill. Start with resource=SKILL.md, then load only the references that skill directs you to.|mode=skill|args=name,resource|timeout=10
cli_anything_status|Report whether Agent8088's isolated CLI-Anything runtime is installed and verified.|mode=cli_anything|timeout=10
cli_anything_setup|Install the pinned CLI-Hub runtime into Agent8088's isolated integration environment. Call status first and use only when CLI-Anything is needed.|mode=cli_anything|timeout=300
cli_anything_list|List the official CLI-Anything catalog as JSON. CLI-Anything setup must already be complete.|mode=cli_anything|timeout=45
cli_anything_search|Search the official CLI-Anything catalog for existing application harnesses. CLI-Anything setup must already be complete.|mode=cli_anything|args=query|timeout=45
cli_anything_info|Show catalog and installation details for one CLI-Anything harness.|mode=cli_anything|args=name|timeout=45
cli_anything_install|Install one reviewed Python CLI-Anything harness into Agent8088's isolated integration environment. Public npm, uv, and generic shell installers are refused.|mode=cli_anything|args=name|timeout=300
cli_anything_update|Update one installed Python CLI-Anything harness inside Agent8088's isolated integration environment.|mode=cli_anything|args=name|timeout=300
cli_anything_uninstall|Remove one installed Python CLI-Anything harness from Agent8088's isolated integration environment.|mode=cli_anything|args=name|timeout=300
cli_anything_skill|Load the packaged SKILL.md for one harness installed in Agent8088's isolated CLI-Anything environment.|mode=cli_anything|args=name|timeout=15
cli_anything_run|Run an installed CLI-Anything harness using its catalog name (for example, gimp) and a structured argument array, never a shell string. Prefer --json in arguments.|mode=cli_anything|args=name,arguments,cwd|arg_types=arguments:array|timeout=300
browse_page|Load a user-supplied web page in a real headless browser and return its text. It is not a routine web-search follow-up.|mode=browser|args=url|timeout=60
create_document|Create a Word, Excel, or PowerPoint file (.docx, .xlsx, .pptx) from plain lines, without writing any code. Each content line is one element. Start a line with "# " for a heading or "## " for a subheading. A line that starts and ends with a pipe character, with cells separated by pipes, is a table row in .docx and a spreadsheet row in .xlsx. "## Sheet: Name" starts a new sheet in .xlsx. "## Title" starts a new slide in .pptx and the lines after it become that slide's bullets. Any other line is a paragraph. Pass an absolute filename. For PDF, or when you need control over layout, write the file with reportlab through execute_shell instead.|mode=write_text|args=filename,content|path_arg=filename|content_arg=content|timeout=30
convert_document|Convert an EXISTING document with LibreOffice: docx, pptx, or xlsx to pdf, or a legacy doc, ppt, or xls to a modern format. Use this instead of writing a script when the user asks to convert a real file that already exists -- do not regenerate the content yourself, this converts the actual file. Args: filename is the absolute path to the existing file, format is the target extension without a dot (pdf, docx, pptx, or xlsx). The result lands next to the source file with the same name and the new extension. Requires LibreOffice to be installed; if it is not, this tool says so plainly. Note: PDF can only be re-exported as PDF (PDF to docx/pptx/xlsx is not supported by LibreOffice headless — use a PDF text-extraction library for that).|mode=write_text|args=filename,format|path_arg=filename|timeout=60
convert_cad|Convert an EXISTING CAD artifact with Agent8088's build123d runtime. Supported targets are step, stl, 3mf, glb, and brep. Use this instead of generating a replacement when the user asks to convert a real file. The result is reopened or parsed before success is reported.|mode=write_text|args=filename,format|path_arg=filename|timeout=300
cad_begin|Start a new supervised, stateful build123d-mcp modelling session for any CAD generation request. project is a bare artifact-directory name, not an absolute path. Supply editable parameters and measurable acceptance requirements as structured objects. This resets prior CAD state and returns the exact workspace.|mode=cad_mcp|args=project,name,parameters,requirements|optional=name,parameters,requirements|arg_types=parameters:object,requirements:object|timeout=60
cad_execute|Add one small, coherent feature operation to the active build123d-mcp session. Use normal build123d Python and show(shape, 'Name') for named solids. Do not submit an entire complex assembly at once. After booleans call cad_measure; checkpoint a known-good state before risky geometry. Build geometry only: use the cad_measure and cad_inspect tools for inspection rather than print() or in-namespace analysis helpers, so the session stays independently replayable. The server blocks arbitrary IO, network, and unsafe imports.|mode=cad_mcp|args=code,checkpoint|optional=checkpoint|content_arg=code|timeout=180
cad_state|Return the active CAD session's named objects, current shape, variables, snapshots, and geometry summary. Use it after beginning, restoring, or resuming a multi-step design.|mode=cad_mcp|read_only=1|timeout=30
cad_measure|Return exact volume, surface area, face/edge/vertex topology, bounding box, center of mass, inertia, and face inventory for the current or named CAD object. Call after every boolean and before claiming a dimension. It does not report the solid count; use cad_validate (n_solids) or cad_inspect (solid_count) for that.|mode=cad_mcp|args=object_name,material|optional=object_name,material|read_only=1|timeout=90
cad_inspect|Inspect the current or named CAD object and check it against the user's requested dimensions, producing an explicit PASS/FAIL. expected is a structured object using ONLY these keys: bbox (a 3-number [x,y,z] array in mm, or an object with any of x/y/z), solid_count (integer), holes (array of {count,axis,diameter,depth,bottom,cbore,spotface}), bosses (array of {count,axis,diameter,height}), patterns (array), section_varying (boolean), tolerance (mm, default 0.1). Any other key is rejected with the supported list. axis and direction accept "Z"/"-Z" or [0,0,1]. holes/bosses/patterns expectations are exhaustive: every recognised group must match exactly one expectation, so declare a central bore alongside a hole pattern, or check only bbox and solid_count and read the inventory from a cad_inspect call with no expected.|mode=cad_mcp|args=object_name,expected|optional=object_name,expected|arg_types=expected:object|read_only=1|timeout=120
cad_validate|Run the build123d-mcp watertight/manifold/B-rep validity gate on the current or named object and report n_solids, open edges, overlaps, and repair reasons. A FAIL must be repaired before export.|mode=cad_mcp|args=object_name|optional=object_name|read_only=1|timeout=120
cad_render|Render and save a high-quality labelled preview of current session geometry. object_names is a comma-separated list of show() names; direction is iso, top, front, or side.|mode=cad_mcp|args=object_names,direction|optional=object_names,direction|timeout=240
cad_snapshot|Save a named geometric checkpoint before a risky modelling operation.|mode=cad_mcp|args=name|timeout=60
cad_restore|Restore a named geometric checkpoint after a failed modelling attempt. Operations made after that checkpoint are also dropped from the session's replayable history, so the exported source matches the restored geometry.|mode=cad_mcp|args=name|timeout=60
cad_compare|Compare two named objects for shape, fit, or alignment, or compare a snapshot to current state. kind is shape, fit, align, or snapshot.|mode=cad_mcp|args=a,b,kind|optional=b,kind|read_only=1|timeout=120
cad_import|Import an existing STEP, STP, STL, or 3MF file into the active supervised CAD session so it can be measured and edited. The file passes Agent8088's sensitive-path checks, is copied into the session workspace, and is bound to a session variable of the same name that cad_execute code can use directly.|mode=cad_mcp|args=filename,name|optional=name|path_arg=filename|timeout=300
cad_last_error|Return the exact line, exception, and repair context for the last failed cad_execute call. Use immediately after an execution failure and repair only that feature.|mode=cad_mcp|read_only=1|timeout=30
cad_export|Finalise a validated CAD session. object_name is one show() name, or "*" for the whole assembly. Exports canonical STEP plus requested STL/3MF into a staging area, saves replayable source and parameters, rebuilds the recorded operations in a clean process and requires identical geometry, independently reopens the STEP through text-to-cad, writes reports and a preview, and only then publishes the artifacts. Nothing is published if any gate fails. formats is an array such as ["step","stl"].|mode=cad_mcp|args=filename,formats,object_name|optional=formats,object_name|arg_types=formats:array|timeout=900
validate_cad_model|Reopen and validate an existing STEP model with build123d and text-to-cad. It writes a JSON geometry report beside the model and, by default, an isometric PNG preview. Set render to false only when the user explicitly does not want a preview.|mode=write_text|args=filename,render|optional=render|path_arg=filename|timeout=300
open_cad_viewer|Open an existing STEP, STP, STL, 3MF, GLB, or DXF artifact in Agent8088's managed text-to-cad CAD Viewer. The Viewer binds only to 127.0.0.1, reuses a healthy existing instance, and returns the exact local review URL. Use after CAD generation and whenever the user asks to open, inspect, measure, explode, clip, or visually review a CAD model. Set open_browser to false only for unattended verification.|mode=read_text|args=filename,open_browser|optional=open_browser|path_arg=filename|timeout=60
