# Pinned, isolated CAD runtime for Python 3.11+. Do not loosen these without rerunning the real
# generation, validation, snapshot, installer and uninstall test matrix.
build123d==0.11.1
cadgen==0.4.28
playwright==1.62.0
bd_warehouse==0.3.0
