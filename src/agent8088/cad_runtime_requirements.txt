# Pinned, isolated CAD runtime for Python 3.11+. Do not loosen these without rerunning the real
# generation, validation, snapshot, installer and uninstall test matrix.
build123d==0.11.1
build123d-mcp==0.3.83
# Upstream excludes this broken macOS wheel only in uv project metadata; repeat
# the constraint here because transitive installs do not inherit tool.uv rules.
cadquery-ocp-novtk!=7.9.3.1.1
cadgen==0.4.28
playwright==1.62.0
